<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_hospital_patient_card" model="ir.actions.act_window">
        <field name="name">Patients Cards</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hr_hospital.patientcard</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="hr_hospital_patient_card_catalog_menu"
              name="Patients cards"
              parent="hr_hospital_patient_catalog_menu"
              action="action_hospital_patient_card"
              sequence="200"/>
</odoo>
